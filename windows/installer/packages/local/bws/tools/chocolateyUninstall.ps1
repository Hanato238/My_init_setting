$ErrorActionPreference = 'Stop'

# Portable package: bws.exe was unzipped into this package's tools folder and
# auto-shimmed by Chocolatey. On uninstall Chocolatey deletes the package folder
# and removes the generated shim, so there is nothing extra to undo here.
